Things that are usually implemented, but ignored so the task requirements are strictly executed and over complecity avoided.

[-] DTO layer (Item)
[-] Pagination for list all
[-] Add FAILDED status and handle it in the listner